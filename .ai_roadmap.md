## Current Phase: Initial site build (Home / Projects / Blog / About)
## Active Architecture State:
- Astro 7.1.6 confirmed (npm list), static output, no adapter (Cloudflare Pages serves static dist/ directly)
- JavaScript throughout, no TypeScript dependency present — plain JS/.astro frontmatter, no type annotations
- Content Layer API confirmed current for this version: src/content.config.mjs, glob() loader, render(entry) from astro:content
- Design system: dark theme, monospace accents on headings/nav, no Tailwind/React/CMS
- Blog schema: title, description, date, tags (default []) — no reading-time field, computed at render time from word count
## Completed Tasks:
(none yet — nothing has been verified by the operator)
## In Progress — Pending Verification:
- [~] Full site scaffold (layout, home, projects, blog index + dynamic post route, about) — verify with: npm run dev, check /, /projects, /blog, /blog/hello-world/, /about
- [~] Content collection schema + sample post build correctly — verify with: npm run build
## Next Immediate Sprint:
- [ ] Operator supplies real bio, project list, and email
- [ ] Confirm Cloudflare Pages build settings (build command npm run build, output dir dist)
- [ ] Replace/delete placeholder hello-world.md once real content exists
