## Current Phase: Single-page redesign (sidebar layout — Aptifolio + brittanychiang.com synthesis)
## Active Architecture State:
- Astro 7.1.6, static output, no adapter, JS throughout (unchanged)
- Site restructured from multi-page (Home/Projects/Blog/About) to single-page: fixed sidebar (identity, numbered anchor nav, socials) + scrollable content column with sections (About/Experience/Projects/Education/Skills/GitHub Activity/Blog teaser)
- /projects and /about retired, redirected via astro.config.mjs redirects to /#projects and /#about (meta-refresh redirect on static output, no true 301 without an adapter)
- /blog and /blog/[...id] unchanged, verified live in production
- Particle-field background + custom cursor unchanged
- New: scroll-reveal, scroll-spy nav, expandable project case studies, live GitHub activity feed
## Completed Tasks:
- [x] Content collection schema + sample post — verified live in production, unaffected by this redesign
## In Progress — Pending Verification:
- [~] Sidebar layout, responsive collapse, scroll-spy, scroll-reveal, redirects — verify with: npm run dev, resize below 980px, confirm /projects and /about redirect, npm run build
- [~] GitHub activity feed — shows setup placeholder until GITHUB_USERNAME is added
- [~] Experience/Education sections removed, project cards now have separate
      Live site / View code links, sidebar socials are icon buttons
      (GitHub/LinkedIn/X/Instagram/Facebook/Email), GitHub Activity redesigned
      as bordered card-rows with relative time — verify with: npm run dev,
      visually check all six social icons render correctly (hand-authored
      SVGs, unverified), and check mobile width (<980px)
- [~] Mobile footer relocated to page bottom, favicon fallback restored,
      OG/Twitter meta added, skip-to-content link + focus-visible styling
      added, robots.txt added — verify with: npm run dev, tab from page
      load to confirm skip link appears first, resize below 980px and
      confirm the footer credit only appears once, at the very bottom
- [~] Mobile footer relocated to page bottom, favicon fallback restored,
      OG/Twitter meta added, skip-to-content link + focus-visible styling
      added, robots.txt added — verify with: npm run dev, tab from page
      load to confirm skip link appears first, resize below 980px and
      confirm the footer credit only appears once, at the very bottom
## Next Immediate Sprint:
- [ ] Operator supplies real content: name, title, bio, experience, education, skills, GitHub username, resume link, photo decision
- [ ] Swap bracketed placeholders for real content
- [ ] Optional Phase 3: Cmd+K command palette, GitHub heatmap embed (separate dependency approval)
