## Current Phase: Feature batch — command palette, gallery pages, theming, polish
## Active Architecture State:
- Astro 7.1.6, static output, no adapter, JS throughout
- Single-page home (sidebar layout) + dedicated /projects and /blog gallery pages, both with tag filtering
- Shared data: src/data/site.js (identity/handles), src/data/projects.js (all project data)
- Shared component: src/components/ProjectCard.astro
- Shared script: src/scripts/tag-filter.js
- Light/dark theme via CSS custom properties + data-theme, no-FOUC inline script, persisted via localStorage
- Particle field + cursor spotlight are theme-aware
- Command palette (Cmd/Ctrl+K), themed 404, reading-progress bar on blog posts
- Cloudflare Web Analytics wired in, needs real token
- NOT added: Astro ClientRouter / View Transitions — deferred as isolated follow-up
## Completed Tasks:
- [x] Content collections + sidebar layout + particle field/cursor — verified live in production prior to this batch
## In Progress — Pending Verification:
- [~] Full feature batch above — verify with: npm run dev, Cmd+K, theme toggle + check particle colors update, tag filters on /projects and /blog, visit an unknown URL for 404, scroll a blog post for progress bar, npm run build
## Next Immediate Sprint:
- [ ] Operator supplies real content: src/data/site.js, src/data/projects.js, skills, GitHub username, Cloudflare Analytics token
- [ ] Follow-up diff: Astro ClientRouter / View Transitions with proper astro:page-load reinit
